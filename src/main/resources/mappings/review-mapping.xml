<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="reviewDao">

	<insert id="addReview" parameterType="reviewvo">
		INSERT INTO CART(
			USER_NO,
			PRODUCT_NO,
			PRODUCT_NAME,
			QUANTITY,
			TOTAL_PRICE,
			PRODUCT_COLOR,
			PRODUCT_SIZE

		) VALUES(
			#{user_no},
			#{product_no},
			#{product_name},
			#{quantity},
			#{total_price},
			#{product_size},
			#{product_color}
		)
	</insert>
	<!-- 전체 카트 삭제 -->
	<!-- <delete id="deleteAllCart">
		DELETE FROM CART
	</delete> -->
	<!-- 리뷰 삭제 -->
	<delete id="deleteCart" parameterType="reviewvo">
		DELETE FROM CART WHERE CART_NO = #{cart_no}
	</delete>
		<!-- 카트 선택 -->
	<select id="cartList" resultType="HashMap">
		SELECT a.cart_no, a.user_no, a.product_no, a.product_name, a.product_size,
		a.product_color, a.quantity, a.total_price, b.delivery_fee, b.discount, b.discount_rate
		FROM CART AS A JOIN PRODUCT AS B
		on a.product_no = b.product_no and user_no = #{user_no};
	</select>
	
	<!--전체 상품리뷰  -->
	<select id="getcartList" parameterType="reviewvo" resultType="reviewvo">
		SELECT * FROM CART WHERE user_no = #{user_no}
	</select>
	
	<!-- 상품 수정을 위한 쿼리 -->
	<update id="updateCart" parameterType="cartvo">
		UPDATE CART   
		SET
		QUANTITY = #{quantity},
		COLOR = #{color},
		CLOTHES_SIZE = #{clothes_size}
		WHERE CART_NO = #{CART_no}
	</update>

</mapper>